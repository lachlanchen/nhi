# Ignore everything by default
*

# Allow directories so git descends into them
!*/

# Keep this ignore file
!.gitignore

# Track core text-based artifacts (scoped to avoid data bloat)
!*.py
!*.md

# Notebooks
!**/*.ipynb         # track notebooks anywhere (code assets)

# Bash scripts (e.g., in versions/**/code, experiments, scripts)
!*.sh
!scripts/
!scripts/**
!.githooks/
!.githooks/**

# Text/CSV/JSON in safe locations
!/*.txt
!/*.csv
!/*.json
!outputs_root/**/*.txt
!outputs_root/**/*.csv
!outputs_root/**/*.json
!versions/**/code/**/*.txt
!versions/**/code/**/*.csv
!versions/**/code/**/*.json
!versions/**/experiments/**/*.txt
!versions/**/experiments/**/*.csv
!versions/**/experiments/**/*.json

# Ignore macOS resource forks and caches
._*
__pycache__/
*.py[cod]
